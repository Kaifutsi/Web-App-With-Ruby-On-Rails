<!DOCTYPE html>
<html>
<head>
  <title>Dashboard</title>
  <link rel="stylesheet" type="text/css" href="/assets/styles.css">
  <style>
    .dashboard-container {
      background-color: #fff;
      padding: 20px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      border-radius: 10px;
      margin: 60px auto;
      max-width: 750px;
    }

    .dashboard h1 {
      font-size: 24px;
      margin-bottom: 20px;
      color: #1a1a1a;
    }

    .dashboard p {
      font-size: 18px;
      color: #1a1a1a;
      margin-bottom: 10px;
    }

    .dashboard-navigation {
      text-align: center;
      margin-top: 20px;
    }

    .dashboard-navigation a {
      text-decoration: none;
      color: #252525;
      font-weight: bold;
      border: 1px solid #252525;
      padding: 10px 20px;
      border-radius: 5px;
      margin: 0 10px;
      display: inline-block;
      transition: background-color 0.2s ease, color 0.2s ease;
    }

    .dashboard-navigation a:hover {
      background-color: #252525;
      color: #fff;
    }
  </style>
</head>
<body>
  <div class="container dashboard-container">
    <h1>Dashboard</h1>
    <% if user_signed_in? %>
      <p>Welcome, <%= current_user.email %>!</p>
      <div class="dashboard-content">
      </div>

      <div class="dashboard-navigation">
        <a href="/">Home</a>
        <a href="<%= user_profile_path %>">Profile</a>
        <% if current_user.admin? %>
          <%= link_to 'All Bookings', all_bookings_path %>
        <% end %>
        <a href="/settings">Settings</a>
        <a href="/logout">Logout</a>
      </div>
    <% else %>
      <p>Please <%= link_to 'Sign In', new_user_session_path %> or <%= link_to 'Register', new_user_registration_path %> if you don't have an account.</p>
    <% end %>
  </div>
</body>
</html>
